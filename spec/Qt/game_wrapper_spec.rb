require "Qt/game_wrapper"

#describe GameWrapper do
  #let(:wrapper) { GameWrapper.new }

  #it "updates when a move is made" do
    #wrapper.should_receive(:update)
    #wrapper.make_move(9)
  #end

#end
